version: '3'

services:
  django-web:
    build: .
    container_name: smart-home
    restart: always
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    env_file:
      - ./.env

networks:
  default:
    external:
      name: website
